# SIF综合处理 → 亚马逊养号网站 集成说明

## 功能概述

将 SIF 综合处理生成的模拟买家评论（内部测试用）直接上传到亚马逊养号网站的直评任务表，实现两个系统的打通。

**流程图**：
```
SIF 综合处理
  ↓ 生成模拟买家评论
  ↓ 点击"上传到养号网站"
  ↓ HTTP API 调用
亚马逊养号网站
  ↓ 存入 direct_review 表
  ↓ 状态: pending (待处理)
Python 自动化工具
  ↓ 自动获取并执行
完成直评任务
```

## 部署步骤

### 1. 养号网站配置

#### 1.1 上传API文件

将以下文件上传到养号网站：

```
e:\桌面\亚马逊养号网站\ymx.com\api\import_reviews_from_sif.php
```

确认文件权限正确（PHP需要可读）。

#### 1.2 验证API地址

API地址通常为：
```
http://你的域名/ymx.com/api/import_reviews_from_sif.php
```

或本地测试：
```
http://localhost/ymx.com/api/import_reviews_from_sif.php
```

#### 1.3 配置API密钥（可选）

在 `import_reviews_from_sif.php` 第 23 行，可以修改密钥：

```php
$validApiKey = 'sif-import-key-2026'; // 修改为你的密钥
```

**注意**：如果不需要密钥验证，将第 13 行改为：
```php
$requireAuth = false; // 不需要登录验证
```

### 2. SIF综合处理配置

#### 2.1 编辑 .env 文件

在 `/e/桌面/SIF综合处理/.env` 文件末尾添加：

```env
# 亚马逊养号网站集成配置
YMX_API_URL=http://localhost/ymx.com/api/import_reviews_from_sif.php
YMX_API_KEY=sif-import-key-2026
YMX_ENABLED=true
```

**配置说明**：
- `YMX_API_URL`：养号网站API完整地址
- `YMX_API_KEY`：与养号网站配置的密钥保持一致
- `YMX_ENABLED`：`true` 启用，`false` 禁用

#### 2.2 重启服务

```powershell
# 停止正在运行的服务（Ctrl+C）
# 重新启动
cd 'E:\桌面\SIF综合处理'
.\.venv\Scripts\Activate.ps1
uvicorn backend.app:app --host 127.0.0.1 --port 8090
```

## 使用方法

### 1. 生成评论

1. 在 SIF 综合处理系统中完成竞品分析
2. 等待生成"模拟买家评论"（通常会生成5条）
3. 在分析结果页查看评论内容

### 2. 上传到养号网站

1. 在"模拟买家评论"栏目右上角，点击**"上传到养号网站"**按钮
2. 确认弹窗中的评论数量
3. 等待上传完成

### 3. 上传结果

成功后会显示：
```
✅ 上传成功！

成功: 5 条
失败: 0 条
```

失败时会显示详细错误信息。

### 4. 养号网站查看

1. 登录亚马逊养号网站：`http://localhost/ymx.com/`
2. 左侧菜单选择**"直评任务"**
3. 查看新增的任务（标记为"SIF自动导入"）
4. 任务状态为**"待处理"**，可由 Python 工具自动执行

## 数据映射关系

| SIF字段 | 养号网站字段 | 说明 |
|---------|-------------|------|
| `title_en` | `review_title` | 英文评论标题 |
| `review_en` | `review_content` | 英文评论内容 |
| `title_cn` + `review_cn` | `remark` | 中文翻译存入备注 |
| `asin` | `link` | 产品链接（自动拼接） |
| `product_name` | `product_name` | 产品名称 |
| `rating` | `star_rating` | 星级（默认5） |
| 当前日期 | `task_date` | 任务日期 |
| SIF用户名 | `operator` | 运营人员 |
| - | `task_status` | 固定为 `pending` |
| - | `has_image` | 固定为 `false` |

## 故障排查

### 问题1：点击按钮无反应

**可能原因**：
- 浏览器控制台有JavaScript错误
- 未重启SIF服务

**解决方法**：
```powershell
# 重启SIF服务
cd 'E:\桌面\SIF综合处理'
.\.venv\Scripts\Activate.ps1
uvicorn backend.app:app --host 127.0.0.1 --port 8090 --reload
```

### 问题2：提示"养号网站集成未启用"

**原因**：`.env` 中 `YMX_ENABLED=false` 或未配置

**解决方法**：
```env
YMX_ENABLED=true
```

### 问题3：提示"连接养号网站超时"

**可能原因**：
1. 养号网站未启动
2. API地址配置错误
3. 网络不通

**解决方法**：
```bash
# 1. 检查养号网站是否运行
curl http://localhost/ymx.com/

# 2. 检查API是否可访问
curl -X POST http://localhost/ymx.com/api/import_reviews_from_sif.php \
  -H "Content-Type: application/json" \
  -d '{"reviews":[]}'

# 应该返回：{"error":"评论数据不能为空"}
```

### 问题4：提示"Invalid API Key"

**原因**：API密钥不匹配

**解决方法**：
1. 检查 `.env` 中的 `YMX_API_KEY`
2. 检查 `import_reviews_from_sif.php` 中的 `$validApiKey`
3. 确保两者完全一致

### 问题5：部分评论上传失败

**查看失败详情**：上传结果弹窗会显示失败原因，常见问题：
- 字段长度超限（标题最多255字符）
- 数据库字段不匹配
- 特殊字符导致SQL错误

**解决方法**：检查养号网站的 `direct_review` 表结构是否与预期一致。

## 安全建议

### 生产环境配置

1. **使用HTTPS**：
```env
YMX_API_URL=https://your-domain.com/ymx.com/api/import_reviews_from_sif.php
```

2. **使用强密钥**：
```env
YMX_API_KEY=使用随机生成的复杂密钥
```

3. **限制访问来源**：
在 `import_reviews_from_sif.php` 开头添加：
```php
// 只允许特定IP访问
$allowed_ips = ['127.0.0.1', '192.168.1.100'];
if (!in_array($_SERVER['REMOTE_ADDR'], $allowed_ips)) {
    http_response_code(403);
    exit('Access denied');
}
```

4. **启用用户认证**：
将 `import_reviews_from_sif.php` 第 13 行改为：
```php
$requireAuth = true; // 需要登录验证
```

## 高级配置

### 自定义任务日期

前端调用时可指定日期：
```javascript
// 在 app.js 的 uploadToYmx() 函数中修改
const customDate = '2026-09-15'; // 自定义日期
body: JSON.stringify({
  job_id: state.jobId, 
  task_date: customDate
}),
```

### 批量上传历史任务

如需批量上传多个历史任务的评论，可以创建脚本调用API：

```python
import requests
import json

# SIF API配置
sif_url = "http://127.0.0.1:8090"
sif_token = "your_sif_token"

# 养号网站API配置
ymx_url = "http://localhost/ymx.com/api/import_reviews_from_sif.php"
ymx_key = "sif-import-key-2026"

# 获取历史任务列表
response = requests.get(
    f"{sif_url}/api/history",
    headers={"Authorization": f"Bearer {sif_token}"}
)
jobs = response.json()["data"]

# 逐个上传
for job in jobs:
    if job["status"] == "completed":
        upload_response = requests.post(
            ymx_url,
            json={"job_id": job["job_id"]},
            headers={
                "Authorization": f"Bearer {ymx_key}",
                "Content-Type": "application/json"
            }
        )
        print(f"Job {job['job_id']}: {upload_response.json()}")
```

## API文档

### 请求格式

**接口地址**：`POST /api/import_reviews_from_sif.php`

**请求头**：
```
Content-Type: application/json
Authorization: Bearer {YMX_API_KEY}  // 可选
```

**请求体**：
```json
{
  "reviews": [
    {
      "title_en": "Great product!",
      "title_cn": "很棒的产品！",
      "review_en": "This is an excellent phone case...",
      "review_cn": "这是一个很棒的手机壳...",
      "asin": "B0FGCY367L",
      "keyword": "phone case",
      "product_name": "iPhone Case",
      "brand": "ABC",
      "rating": 5
    }
  ],
  "task_date": "2026-09-11",
  "operator": "运营A"
}
```

**响应格式**（成功）：
```json
{
  "success": true,
  "message": "评论导入完成",
  "success_count": 5,
  "fail_count": 0,
  "fail_details": [],
  "inserted_ids": [123, 124, 125, 126, 127]
}
```

**响应格式**（失败）：
```json
{
  "error": "评论数据不能为空",
  "details": "具体错误信息"
}
```

## 联系支持

如有问题，请检查：
1. SIF服务日志
2. 养号网站PHP错误日志
3. 浏览器开发者工具的Network和Console

---

**版本**：1.0.0  
**更新日期**：2026-09-11  
**兼容性**：SIF综合处理 v0.1.0 + 亚马逊养号网站
